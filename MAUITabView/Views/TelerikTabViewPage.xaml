<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="MAUITabView.TelerikTabViewPage"
             xmlns:local="clr-namespace:MAUITabView"
              xmlns:telerik="http://schemas.telerik.com/2022/xaml/maui"
             Title="TelerikTabViewPage">

    <ContentPage.BindingContext>
        <local:EmployeeDetails/>
    </ContentPage.BindingContext>


    <telerik:RadTabView x:Name="telerikTabView" Loaded="TelerikTabView_Loaded" IsHeaderScrollable="True" >
        <telerik:TabViewItem HeaderText="Tab1">
            <Grid>
                <ListView ItemsSource="{Binding EmployeeDetailsList}" x:DataType="{x:Type local:EmployeeDetails}">
                    <ListView.ItemTemplate >
                        <DataTemplate x:DataType="{x:Type local:Employee}">
                            <ViewCell>
                                <Label Text="{Binding Name}" VerticalTextAlignment="Center" HorizontalTextAlignment="Center" HeightRequest="40"/>
                            </ViewCell>
                        </DataTemplate>
                    </ListView.ItemTemplate>
                </ListView>
            </Grid>
        </telerik:TabViewItem>
        <telerik:TabViewItem HeaderText="Tab2">
            <Grid>
                <ListView ItemsSource="{Binding EmployeeDetailsList}" x:DataType="{x:Type local:EmployeeDetails}">
                    <ListView.ItemTemplate >
                        <DataTemplate x:DataType="{x:Type local:Employee}">
                            <ViewCell>
                                <Label Text="{Binding Name}" VerticalTextAlignment="Center" HorizontalTextAlignment="Center" HeightRequest="40"/>
                            </ViewCell>
                        </DataTemplate>
                    </ListView.ItemTemplate>
                </ListView>
            </Grid>
        </telerik:TabViewItem>
        <telerik:TabViewItem HeaderText="Tab3">
            <Grid>
                <ListView ItemsSource="{Binding EmployeeDetailsList}" x:DataType="{x:Type local:EmployeeDetails}">
                    <ListView.ItemTemplate >
                        <DataTemplate x:DataType="{x:Type local:Employee}">
                            <ViewCell>
                                <Label Text="{Binding Name}" VerticalTextAlignment="Center" HorizontalTextAlignment="Center" HeightRequest="40"/>
                            </ViewCell>
                        </DataTemplate>
                    </ListView.ItemTemplate>
                </ListView>
            </Grid>
        </telerik:TabViewItem>
        <telerik:TabViewItem HeaderText="Tab4">
            <Grid>
                <ListView ItemsSource="{Binding EmployeeDetailsList}" x:DataType="{x:Type local:EmployeeDetails}">
                    <ListView.ItemTemplate >
                        <DataTemplate x:DataType="{x:Type local:Employee}">
                            <ViewCell>
                                <Label Text="{Binding Name}" VerticalTextAlignment="Center" HorizontalTextAlignment="Center" HeightRequest="40"/>
                            </ViewCell>
                        </DataTemplate>
                    </ListView.ItemTemplate>
                </ListView>
            </Grid>
        </telerik:TabViewItem>
        <telerik:TabViewItem HeaderText="Tab5">
            <Grid>
                <ListView ItemsSource="{Binding EmployeeDetailsList}" x:DataType="{x:Type local:EmployeeDetails}">
                    <ListView.ItemTemplate >
                        <DataTemplate x:DataType="{x:Type local:Employee}">
                            <ViewCell>
                                <Label Text="{Binding Name}" VerticalTextAlignment="Center" HorizontalTextAlignment="Center" HeightRequest="40"/>
                            </ViewCell>
                        </DataTemplate>
                    </ListView.ItemTemplate>
                </ListView>
            </Grid>
        </telerik:TabViewItem>
        <telerik:TabViewItem HeaderText="Tab6">
            <Grid>
                <ListView ItemsSource="{Binding EmployeeDetailsList}" x:DataType="{x:Type local:EmployeeDetails}">
                    <ListView.ItemTemplate >
                        <DataTemplate x:DataType="{x:Type local:Employee}">
                            <ViewCell>
                                <Label Text="{Binding Name}" VerticalTextAlignment="Center" HorizontalTextAlignment="Center" HeightRequest="40"/>
                            </ViewCell>
                        </DataTemplate>
                    </ListView.ItemTemplate>
                </ListView>
            </Grid>
        </telerik:TabViewItem>
        <telerik:TabViewItem HeaderText="Tab7">
            <Grid>
                <ListView ItemsSource="{Binding EmployeeDetailsList}" x:DataType="{x:Type local:EmployeeDetails}">
                    <ListView.ItemTemplate >
                        <DataTemplate x:DataType="{x:Type local:Employee}">
                            <ViewCell>
                                <Label Text="{Binding Name}" VerticalTextAlignment="Center" HorizontalTextAlignment="Center" HeightRequest="40"/>
                            </ViewCell>
                        </DataTemplate>
                    </ListView.ItemTemplate>
                </ListView>
            </Grid>
        </telerik:TabViewItem>
        <telerik:TabViewItem HeaderText="Tab8">
            <Grid>
                <ListView ItemsSource="{Binding EmployeeDetailsList}" x:DataType="{x:Type local:EmployeeDetails}">
                    <ListView.ItemTemplate >
                        <DataTemplate x:DataType="{x:Type local:Employee}">
                            <ViewCell>
                                <Label Text="{Binding Name}" VerticalTextAlignment="Center" HorizontalTextAlignment="Center" HeightRequest="40"/>
                            </ViewCell>
                        </DataTemplate>
                    </ListView.ItemTemplate>
                </ListView>
            </Grid>
        </telerik:TabViewItem>
        <telerik:TabViewItem HeaderText="Tab9">
            <Grid>
                <ListView ItemsSource="{Binding EmployeeDetailsList}" x:DataType="{x:Type local:EmployeeDetails}">
                    <ListView.ItemTemplate >
                        <DataTemplate x:DataType="{x:Type local:Employee}">
                            <ViewCell>
                                <Label Text="{Binding Name}" VerticalTextAlignment="Center" HorizontalTextAlignment="Center" HeightRequest="40"/>
                            </ViewCell>
                        </DataTemplate>
                    </ListView.ItemTemplate>
                </ListView>
            </Grid>
        </telerik:TabViewItem>
        <telerik:TabViewItem HeaderText="Tab10">
            <Grid>
                <ListView ItemsSource="{Binding EmployeeDetailsList}" x:DataType="{x:Type local:EmployeeDetails}">
                    <ListView.ItemTemplate >
                        <DataTemplate x:DataType="{x:Type local:Employee}">
                            <ViewCell>
                                <Label Text="{Binding Name}" VerticalTextAlignment="Center" HorizontalTextAlignment="Center" HeightRequest="40"/>
                            </ViewCell>
                        </DataTemplate>
                    </ListView.ItemTemplate>
                </ListView>
            </Grid>
        </telerik:TabViewItem>
        <telerik:TabViewItem HeaderText="Tab11">
            <Grid>
                <ListView ItemsSource="{Binding EmployeeDetailsList}" x:DataType="{x:Type local:EmployeeDetails}">
                    <ListView.ItemTemplate >
                        <DataTemplate x:DataType="{x:Type local:Employee}">
                            <ViewCell>
                                <Label Text="{Binding Name}" VerticalTextAlignment="Center" HorizontalTextAlignment="Center" HeightRequest="40"/>
                            </ViewCell>
                        </DataTemplate>
                    </ListView.ItemTemplate>
                </ListView>
            </Grid>
        </telerik:TabViewItem>
        <telerik:TabViewItem HeaderText="Tab12">
            <Grid>
                <ListView ItemsSource="{Binding EmployeeDetailsList}" x:DataType="{x:Type local:EmployeeDetails}">
                    <ListView.ItemTemplate >
                        <DataTemplate x:DataType="{x:Type local:Employee}">
                            <ViewCell>
                                <Label Text="{Binding Name}" VerticalTextAlignment="Center" HorizontalTextAlignment="Center" HeightRequest="40"/>
                            </ViewCell>
                        </DataTemplate>
                    </ListView.ItemTemplate>
                </ListView>
            </Grid>
        </telerik:TabViewItem>
        <telerik:TabViewItem HeaderText="Tab13">
            <Grid>
                <ListView ItemsSource="{Binding EmployeeDetailsList}" x:DataType="{x:Type local:EmployeeDetails}">
                    <ListView.ItemTemplate >
                        <DataTemplate x:DataType="{x:Type local:Employee}">
                            <ViewCell>
                                <Label Text="{Binding Name}" VerticalTextAlignment="Center" HorizontalTextAlignment="Center" HeightRequest="40"/>
                            </ViewCell>
                        </DataTemplate>
                    </ListView.ItemTemplate>
                </ListView>
            </Grid>
        </telerik:TabViewItem>
        <telerik:TabViewItem HeaderText="Tab14">
            <Grid>
                <ListView ItemsSource="{Binding EmployeeDetailsList}" x:DataType="{x:Type local:EmployeeDetails}">
                    <ListView.ItemTemplate >
                        <DataTemplate x:DataType="{x:Type local:Employee}">
                            <ViewCell>
                                <Label Text="{Binding Name}" VerticalTextAlignment="Center" HorizontalTextAlignment="Center" HeightRequest="40"/>
                            </ViewCell>
                        </DataTemplate>
                    </ListView.ItemTemplate>
                </ListView>
            </Grid>
        </telerik:TabViewItem>
        <telerik:TabViewItem HeaderText="Tab15">
            <Grid>
                <ListView ItemsSource="{Binding EmployeeDetailsList}" x:DataType="{x:Type local:EmployeeDetails}">
                    <ListView.ItemTemplate >
                        <DataTemplate x:DataType="{x:Type local:Employee}">
                            <ViewCell>
                                <Label Text="{Binding Name}" VerticalTextAlignment="Center" HorizontalTextAlignment="Center" HeightRequest="40"/>
                            </ViewCell>
                        </DataTemplate>
                    </ListView.ItemTemplate>
                </ListView>
            </Grid>
        </telerik:TabViewItem>
        <telerik:TabViewItem HeaderText="Tab16">
            <Grid>
                <ListView ItemsSource="{Binding EmployeeDetailsList}" x:DataType="{x:Type local:EmployeeDetails}">
                    <ListView.ItemTemplate >
                        <DataTemplate x:DataType="{x:Type local:Employee}">
                            <ViewCell>
                                <Label Text="{Binding Name}" VerticalTextAlignment="Center" HorizontalTextAlignment="Center" HeightRequest="40"/>
                            </ViewCell>
                        </DataTemplate>
                    </ListView.ItemTemplate>
                </ListView>
            </Grid>
        </telerik:TabViewItem>
        <telerik:TabViewItem HeaderText="Tab17">
            <Grid>
                <ListView ItemsSource="{Binding EmployeeDetailsList}" x:DataType="{x:Type local:EmployeeDetails}">
                    <ListView.ItemTemplate >
                        <DataTemplate x:DataType="{x:Type local:Employee}">
                            <ViewCell>
                                <Label Text="{Binding Name}" VerticalTextAlignment="Center" HorizontalTextAlignment="Center" HeightRequest="40"/>
                            </ViewCell>
                        </DataTemplate>
                    </ListView.ItemTemplate>
                </ListView>
            </Grid>
        </telerik:TabViewItem>
        <telerik:TabViewItem HeaderText="Tab18">
            <Grid>
                <ListView ItemsSource="{Binding EmployeeDetailsList}" x:DataType="{x:Type local:EmployeeDetails}">
                    <ListView.ItemTemplate >
                        <DataTemplate x:DataType="{x:Type local:Employee}">
                            <ViewCell>
                                <Label Text="{Binding Name}" VerticalTextAlignment="Center" HorizontalTextAlignment="Center" HeightRequest="40"/>
                            </ViewCell>
                        </DataTemplate>
                    </ListView.ItemTemplate>
                </ListView>
            </Grid>
        </telerik:TabViewItem>
        <telerik:TabViewItem HeaderText="Tab19">
            <Grid>
                <ListView ItemsSource="{Binding EmployeeDetailsList}" x:DataType="{x:Type local:EmployeeDetails}">
                    <ListView.ItemTemplate >
                        <DataTemplate x:DataType="{x:Type local:Employee}">
                            <ViewCell>
                                <Label Text="{Binding Name}" VerticalTextAlignment="Center" HorizontalTextAlignment="Center" HeightRequest="40"/>
                            </ViewCell>
                        </DataTemplate>
                    </ListView.ItemTemplate>
                </ListView>
            </Grid>
        </telerik:TabViewItem>
        <telerik:TabViewItem HeaderText="Tab20">
            <Grid>
                <ListView ItemsSource="{Binding EmployeeDetailsList}" x:DataType="{x:Type local:EmployeeDetails}">
                    <ListView.ItemTemplate >
                        <DataTemplate x:DataType="{x:Type local:Employee}">
                            <ViewCell>
                                <Label Text="{Binding Name}" VerticalTextAlignment="Center" HorizontalTextAlignment="Center" HeightRequest="40"/>
                            </ViewCell>
                        </DataTemplate>
                    </ListView.ItemTemplate>
                </ListView>
            </Grid>
        </telerik:TabViewItem>
        

    </telerik:RadTabView>


</ContentPage>